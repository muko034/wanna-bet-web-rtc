# Issue Tracker

Tasks are tracked as files under `docs/spec/`, one directory per feature area (e.g. `docs/spec/round-engine/`). Each directory has a `SPEC.md` overview plus numbered task files (e.g. `16-bettor-places-a-bet.md`).

